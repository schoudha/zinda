import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Deprecated: web runtime has been replaced by native Android implementation.
  reactStrictMode: false,
};

export default nextConfig;
